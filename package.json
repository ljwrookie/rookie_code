{
  "name": "rookie-code-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev:cli": "pnpm --filter rookie-code dev",
    "start:cli": "node packages/cli/dist/index.js",
    "lint": "pnpm -r lint",
    "test": "pnpm -r --if-present test",
    "test:run": "pnpm -r --if-present test:run"
  }
}
